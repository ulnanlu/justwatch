services:
  app:
    image: ghcr.io/ulnanlu/justwatch:latest
    # Uncomment to build locally instead:
    # build:
    #   context: .
    #   dockerfile: ./backend/Dockerfile
    ports:
      - "43434:43434"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
